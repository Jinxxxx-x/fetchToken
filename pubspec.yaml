name: token_server
description: Gerador de tokens para conexão do app Tô Na Rede com o servidor Agora.io.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  agora_rtc_engine: ^6.2.6
  cupertino_icons: ^1.0.2
  agora_token_service: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
